<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        TABLA DE DATOS
        ==============
        Es la pieza central del producto: el inventario se vive aqui.

        Decisiones:
          - Solo lineas horizontales. Las verticales convierten la tabla en
            una hoja de calculo y anaden ruido sin aportar lectura.
          - Cabecera fija, sin relieve, con separacion clara del cuerpo.
          - Fila de 38 px: densidad de herramienta profesional, no de app movil.
          - Sin filas alternas: con hover y lineas finas la lectura ya es
            correcta, y el rayado gris envejece mucho una interfaz.
          - Virtualizacion activada: es lo que permite mover decenas de miles
            de articulos sin que la pantalla se arrastre.
    -->

    <!-- Separador de la cabecera -->
    <Style x:Key="Grid.ColumnHeaderGripper" TargetType="Thumb">
        <Setter Property="Width" Value="6"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Cursor" Value="SizeWE"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border Background="{TemplateBinding Background}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Grid.ColumnHeader" TargetType="DataGridColumnHeader">
        <Setter Property="FontFamily" Value="{StaticResource FontUI}"/>
        <Setter Property="FontSize" Value="{StaticResource Size.Caption}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
        <Setter Property="Background" Value="{StaticResource SurfaceAlt}"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="Padding" Value="12,0,12,0"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridColumnHeader">
                    <Grid>
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource Border}"
                                BorderThickness="0,0,0,1"
                                SnapsToDevicePixels="True">
                            <Grid Margin="{TemplateBinding Padding}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter VerticalAlignment="Center"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                                <!-- Indicador de orden: triangulo pequeno, no una
                                     flecha grande. Informa sin llamar la atencion. -->
                                <Path x:Name="sort" Grid.Column="1" Margin="6,1,0,0"
                                      VerticalAlignment="Center"
                                      Fill="{StaticResource TextSubtle}"
                                      Visibility="Collapsed"
                                      Data="M 0 0 L 8 0 L 4 5 Z"/>
                            </Grid>
                        </Border>
                        <Thumb x:Name="PART_RightHeaderGripper"
                               HorizontalAlignment="Right"
                               Style="{StaticResource Grid.ColumnHeaderGripper}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource SurfaceHover}"/>
                        </Trigger>
                        <Trigger Property="SortDirection" Value="Ascending">
                            <Setter TargetName="sort" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="sort" Property="RenderTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="180" CenterX="4" CenterY="2.5"/>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Foreground" Value="{StaticResource Text}"/>
                        </Trigger>
                        <Trigger Property="SortDirection" Value="Descending">
                            <Setter TargetName="sort" Property="Visibility" Value="Visible"/>
                            <Setter Property="Foreground" Value="{StaticResource Text}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Grid.Cell" TargetType="DataGridCell">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{StaticResource Text}"/>
        <Setter Property="Padding" Value="12,0,12,0"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Grid.Row" TargetType="DataGridRow">
        <Setter Property="Background" Value="{StaticResource Surface}"/>
        <Setter Property="Height" Value="{StaticResource Row.Height}"/>
        <Setter Property="FontFamily" Value="{StaticResource FontUI}"/>
        <Setter Property="FontSize" Value="{StaticResource Size.Body}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridRow">
                    <Border x:Name="bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{StaticResource Border}"
                            BorderThickness="0,0,0,1"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <!-- Marca de seleccion a la izquierda: identifica la
                                 fila activa sin teñir toda la linea de azul. -->
                            <Border x:Name="mark" Width="3" HorizontalAlignment="Left"
                                    Background="{StaticResource Accent}" Visibility="Collapsed"/>
                            <DataGridCellsPresenter/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource SurfaceHover}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource AccentSoft}"/>
                            <Setter TargetName="mark" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Grid.Table" TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource Surface}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="RowBackground" Value="{StaticResource Surface}"/>
        <Setter Property="GridLinesVisibility" Value="None"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="CanUserResizeRows" Value="False"/>
        <Setter Property="CanUserReorderColumns" Value="False"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource Grid.ColumnHeader}"/>
        <Setter Property="CellStyle" Value="{StaticResource Grid.Cell}"/>
        <Setter Property="RowStyle" Value="{StaticResource Grid.Row}"/>
        <Setter Property="RowHeight" Value="{StaticResource Row.Height}"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <!-- Sin virtualizacion, 20.000 articulos crean 20.000 filas en memoria
             y la pantalla tarda segundos en responder. -->
        <Setter Property="EnableRowVirtualization" Value="True"/>
        <Setter Property="EnableColumnVirtualization" Value="True"/>
        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
        <Setter Property="VirtualizingPanel.ScrollUnit" Value="Pixel"/>
    </Style>

    <!-- ============================================================
         DISTINTIVOS DE ESTADO
         Nunca se usa el color como unica senal: cada distintivo lleva
         siempre su texto.
         ============================================================ -->

    <Style x:Key="Badge.Base" TargetType="Border">
        <Setter Property="CornerRadius" Value="{StaticResource Radius.Sm}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="7,2,7,2"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <Style x:Key="Badge.Success" TargetType="Border" BasedOn="{StaticResource Badge.Base}">
        <Setter Property="Background" Value="{StaticResource SuccessSoft}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SuccessBorder}"/>
    </Style>

    <Style x:Key="Badge.Warning" TargetType="Border" BasedOn="{StaticResource Badge.Base}">
        <Setter Property="Background" Value="{StaticResource WarningSoft}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WarningBorder}"/>
    </Style>

    <Style x:Key="Badge.Danger" TargetType="Border" BasedOn="{StaticResource Badge.Base}">
        <Setter Property="Background" Value="{StaticResource DangerSoft}"/>
        <Setter Property="BorderBrush" Value="{StaticResource DangerBorder}"/>
    </Style>

    <Style x:Key="Badge.Neutral" TargetType="Border" BasedOn="{StaticResource Badge.Base}">
        <Setter Property="Background" Value="{StaticResource SurfaceSunken}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
    </Style>

    <Style x:Key="Badge.Info" TargetType="Border" BasedOn="{StaticResource Badge.Base}">
        <Setter Property="Background" Value="{StaticResource AccentSoft}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AccentSoftBorder}"/>
    </Style>

    <Style x:Key="Badge.Text" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource FontUI}"/>
        <Setter Property="FontSize" Value="{StaticResource Size.Micro}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <!--
        Simbolo que acompana al texto del distintivo.

        Es la segunda senal, ademas del texto: quien no distingue el verde del
        rojo sigue viendo un simbolo distinto por estado. El color es la
        tercera, y nunca la unica.
    -->
    <Style x:Key="Badge.Glyph" TargetType="TextBlock" BasedOn="{StaticResource Badge.Text}">
        <Setter Property="FontFamily" Value="{StaticResource FontIcons}"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="Margin" Value="0,0,5,0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

</ResourceDictionary>
