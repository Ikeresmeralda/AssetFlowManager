<?xml version="1.0" encoding="utf-8"?>
<!--
    Sólo para compilaciones de depuracion. Sustituye al fichero del mismo
    nombre en src/main y permite trafico en claro contra el bucle local, que es
    la direccion del servidor de desarrollo visto desde el emulador
    (http://10.0.2.2:5171). La compilacion release nunca lleva este fichero, asi
    que la aplicacion publicada sigue sin admitir HTTP.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <!-- La maquina anfitriona vista desde el emulador de Android. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
